plugins {
  id "com.android.application"
}

description = """
An accessible calculator.
"""

dependencies {
  implementation project(":common")
}

apply from: rootProject.file("nbp-all.gradle")
nbp.applicationPrefix = "NBP_Calculator"
apply from: rootProject.file("nbp-app.gradle")
