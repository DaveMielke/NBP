apply from: rootProject.file("nbp-app-config.gradle")
apply from: rootProject.file("nbp-app-sign.gradle")

android {
  buildTypes {
    release {
      minifyEnabled false
      proguardFile getDefaultProguardFile("proguard-android.txt")
    }
  }
}
