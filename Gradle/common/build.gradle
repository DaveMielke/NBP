plugins {
  id "com.android.library"
}

description = """
Common objects used by all ${rootProject.name} projects.
"""

android {
  compileSdkVersion "android-29"
  buildToolsVersion "29.0.3"

  defaultConfig {
    targetSdkVersion 29
    minSdkVersion 16
  }
}
