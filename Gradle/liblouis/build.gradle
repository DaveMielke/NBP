plugins {
  id "com.android.library"
}

description = """
Java objects that wrap the LibLouis native code.
"""

dependencies {
  implementation project(":duxbury")
}

apply from: rootProject.file("nbp-build.gradle")
apply from: rootProject.file("nbp-jni.gradle")
