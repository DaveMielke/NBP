Verify:
  USB Ethernet adapter
  SIM indicators
  braille enable/disable
  braille monitor

Document
  speech volume dependent on system volume
  locale-based character table autodetection
  override files
  how kbd and ui navigation can get out of sync

Device:
   unique USB serial numbers
   unique Wi-Fi mac addresses

User Interface
  LibLouis
  make SD unmount easier
  input service doesn't restart immediately after reinstall
  arrowed-to item blocks scrolling
  Chord for key binding list

Editor
  interpret typed characters rather than keyboard events

Launcher
  routing key scroll bar
  bindable routing keys
  typing should present sublist
