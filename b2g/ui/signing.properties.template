#key.store = /path/to/keystore
key.store = ../security/B2G-RevC.jks

#key.store.password = password-for-keystore

#key.alias = name-of-key
key.alias = platform

#key.alias.password = password-for-key

